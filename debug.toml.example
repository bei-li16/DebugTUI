version = 2
watch = []
[gdb]
executable = "gdb"
[program]
elf = "./build/app.elf"
source_root = "."
[target]
mode = "remote"
endpoint = "127.0.0.1:3333"
[session]
on_exit = "detach"

# To use a tools environment instead, remove the [gdb] section above and add:
# [tools]
# profile = "./tools/debug-env.toml"
